<%= form_for(@card_pile) do |f| %>
  <% if @card_pile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_pile.errors.count, "error") %> prohibited this card_pile from being saved:</h2>

      <ul>
      <% @card_pile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
