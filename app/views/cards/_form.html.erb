<%= form_for(@card) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
		<%= f.label :card_template_id %><br>
		<%= f.select(:card_template_id, options_from_collection_for_select( CardTemplate.all, :id, :name )) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
